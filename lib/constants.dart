import 'package:flutter/material.dart';

//fun createPaletteSync(bitmap: Bitmap): Palette = Palette.from(bitmap).generate()

const backgroundColor = Color(0xff78bf9a);
const myColor = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment(10, 3),
  colors: <Color>[
    Colors.white,
    Color(0xffe67676),
  ],
);

//, ffcc7b--beige
// fff50519
