import 'package:flutter/material.dart';

class SearchBio extends StatelessWidget{


  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          Text('bio'),
          Text('prompts')
        ],
      ),
    );
  }


}